#include "../include/move.h"

void fill_move(Game_state *current_state, char arg[50]){
    Flag f = current_state->move;
    switch (f) {
        case SET:

            break;
        case WAIT:
            break;
        case VALID:
            break;
        case INVALID:
            break;
    }
}
